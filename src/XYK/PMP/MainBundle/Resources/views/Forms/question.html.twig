{% extends 'MainBundle:Master:master.html.twig' %}
{% block content %}
            
 <div class="panel panel-primary">
  <div class="panel-heading">
      <h3 class="panel-title">{{ Title }}</h3>
  </div>
  <div class="panel-body">
      <h6>
        
            <div class="radio">
                <label>
                    <input type="radio"  name="question" value="{{Option1.id}}" {% if Answered  %} disabled {%if Answer ==1 %} checked {%endif%} {% endif %}>
                    {{Option1.description}}
                    {%if (Answered) and (Answer ==1)%}
                        {%if Option1.correct%} 
                            <span class="label label-success">&#10004;</span>     
                        {%else%}
                            <span class="label label-danger">&#10008;</span>
                        {%endif%}
                    {%endif%}  
                </label>
            </div>
            <div class="radio">
                <label>
                    <input type="radio"  name="question" value="{{Option2.id}}" {% if Answered  %} disabled {%if Answer ==2 %} checked {%endif%} {% endif %}>
                    {{Option2.description}}
                    {%if (Answered) and (Answer ==2)%}
                        {%if Option1.correct%} 
                            <span class="label label-success">&#10004;</span>     
                        {%else%}
                            <span class="label label-danger">&#10008;</span>
                        {%endif%}
                    {%endif%}  
                </label>
            </div>
            <div class="radio">
                <label>
          
                    <input type="radio"  name="question" value="{{Option3.id}}" {% if Answered  %} disabled {%if Answer ==3 %} checked {%endif%} {% endif %}>
                    {{Option3.description}}
                    {%if (Answered) and (Answer ==3)%}
                        {%if Option1.correct%} 
                            <span class="label label-success">&#10004;</span>     
                        {%else%}
                            <span class="label label-danger">&#10008;</span>
                        {%endif%}
                    {%endif%}  
                </label>
            </div>
            <div class="radio">
                <label>
                    <input type="radio"  name="question" value="{{Option4.id}}" {% if Answered  %} disabled {%if Answer ==4 %} checked {%endif%} {% endif %}>
                    {{Option4.description}}
                    {%if (Answered) and (Answer == 4)%}
                        {%if Option1.correct%} 
                            <span class="label label-success">&#10004;</span>     
                        {%else%}
                            <span class="label label-danger">&#10008;</span>
                        {%endif%}
                    {%endif%}  
                </label>
            </div>
    </h6>
        <div class="btn-group" role="group" aria-label="...">
            <button id="btn_previous" type="button" class="btn btn-primary">Anterior</button>
            <button id="btn_next" type="button" class="btn btn-primary">Siguiente</button>
            {% if ShowExplanation%}
                <button id="btn_explanation" type="button" class="btn btn-info" onclick="$( '#panel_explanation' ).toggle(); return false;">Explicación</button>
            {% endif %}
            <button id="btn_questions" type="button" class="btn btn-default">Preguntas</button>
            <button id="btn_finish" type="button" class="btn btn-success">Evaluar</button>
        </div>

  </div>
    
</div>  
<div id="panel_explanation" class="panel panel-info" style="display: none">
  <div class="panel-heading">
    <h3 class="panel-title">Explicación</h3>
  </div>
  <div class="panel-body">
     {{ Explanation}}
  </div>
</div>  
        

{% endblock content %} 
  
  


